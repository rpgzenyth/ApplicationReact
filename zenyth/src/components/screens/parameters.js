import React from 'react';
import { ContentDiv } from '../../config/styled';

const Parameters = () => {
    return (
        <ContentDiv>
            <h1>Parameters</h1>
        </ContentDiv>
    );
};

export default Parameters;